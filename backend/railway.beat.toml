"$schema" = "https://railway.app/railway.schema.json"

[build]
builder = "NIXPACKS"

[deploy]
startCommand = "cd backend && celery -A celery_app beat -l info"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
